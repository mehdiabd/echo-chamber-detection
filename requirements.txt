# Core graph and analysis tools
networkx
python-louvain==0.16  # Louvain community detection
node2vec        # Node2Vec graph embedding

# ML & clustering
numpy<2
scikit-learn==1.3.0    # For k-means, PCA, evaluation

# Visualization
matplotlib==3.8.0
pyvis==0.3.2

# Search & retrieval
# Must stay on 8.x: the server rejects the v9 compatibility header with
# media_type_header_exception "Accept version must be either version 8 or 7,
# but found 9". Unpinned, pip installed 9.5.0 and every call 400d.
elasticsearch>=8.0,<9.0
ollama
requests
python-dateutil

# Environment management
python-dotenv==1.0.0

# HTTP API
fastapi==0.115.6
uvicorn==0.32.1
